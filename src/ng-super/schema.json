{
	"$schema": "http://json-schema.org/schema",
	"id": "megaModule",
	"type": "object",
	"properties": {
		"name": {
			"type": "string",
			"$default": {
				"$source": "argv",
				"index": 0
			},
			"description": "Mega Module Name",
			"x-prompt": "What is the mega entity name?"
		},
		"store": {
			"type": "boolean",
			"description": "This property for including the store in the module.",
			"default": false,
			"x-prompt": "Do you want to setup the store(ngrx) in the module?"

		}
	},
	"required": [
		"name"
	]
}