{
	"$schema": "http://json-schema.org/schema",
	"id": "megaModule",
	"type": "object",
	"properties": {
		"name": {
			"type": "string",
			"$default": {
				"$source": "argv",
				"index": 0
			},
			"description": "Mega Module Name",
			"x-prompt": "What is the mega entity name?"
		},
		"store": {
			"type": "boolean",
			"default":"no-store",
			"description": "Create the new files at the top level of the current project root. ",
			"x-prompt": "Do you want to use store(ngrx)?"
		}
	},
	"required": [
		"name"
	]
}